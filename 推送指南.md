# Git 推送指南

本文档说明如何将代码改动推送到 GitHub。

## 📁 项目结构说明

本项目包含两个 Git 仓库：
- **主仓库**（bird_download）：包含数据处理脚本、配置文件、鸟类数据等
- **子模块**（feather-flash-quiz）：前端应用代码

## 🚀 推送场景

### 场景 1：只修改了主仓库文件

**适用情况**：修改了工具脚本、数据文件、配置文件等

```bash
# 进入主仓库目录
cd /Users/my/Desktop/Code/小鸟记忆卡

# 查看改动
git status

# 添加所有改动
git add -A

# 提交改动（用中文描述）
git commit -m "描述你的改动内容"

# 推送到远程
git push origin main
```

**示例**：
```bash
git commit -m "feat: 新增广州地区鸟单"
git commit -m "fix: 修复鸟叫声下载脚本的编码问题"
git commit -m "chore: 更新鸟类数据"
```

---

### 场景 2：只修改了前端代码（子模块）

**适用情况**：修改了 feather-flash-quiz 目录下的前端代码

```bash
# 第一步：推送子模块
cd /Users/my/Desktop/Code/小鸟记忆卡/feather-flash-quiz

# 查看改动
git status

# 添加所有改动
git add -A

# 提交改动
git commit -m "描述前端的改动"

# 推送子模块
git push origin main

# 第二步：更新主仓库的子模块引用
cd /Users/my/Desktop/Code/小鸟记忆卡

# 添加子模块引用更新
git add feather-flash-quiz

# 提交
git commit -m "chore: 更新子模块引用"

# 推送主仓库
git push origin main
```

**⚠️ 重要**：子模块必须先推送，然后再推送主仓库！

---

### 场景 3：同时修改了主仓库和子模块

**适用情况**：既改了数据/脚本，又改了前端代码

```bash
# 第一步：先处理子模块
cd /Users/my/Desktop/Code/小鸟记忆卡/feather-flash-quiz
git add -A
git commit -m "描述前端改动"
git push origin main

# 第二步：再处理主仓库
cd /Users/my/Desktop/Code/小鸟记忆卡
git add -A
git commit -m "描述主仓库改动（包含子模块更新）"
git push origin main
```

---

## 🔍 常用检查命令

### 查看当前状态
```bash
git status
```

### 查看具体改动内容
```bash
# 查看未暂存的改动
git diff

# 查看已暂存的改动
git diff --staged

# 查看某个文件的改动
git diff 文件路径
```

### 查看提交历史
```bash
# 查看最近 5 条提交
git log --oneline -5

# 查看详细提交历史
git log -3
```

### 撤销操作
```bash
# 撤销还未 commit 的改动
git restore 文件名

# 撤销所有未 commit 的改动（危险！）
git restore .

# 取消已经 add 的文件
git restore --staged 文件名
```

---

## 📝 Commit 信息规范

建议使用以下格式：

```
类型: 简短描述

详细说明（可选）
```

### 常用类型
- `feat`: 新功能
- `fix`: 修复 bug
- `chore`: 日常维护、更新数据
- `docs`: 文档更新
- `refactor`: 代码重构
- `style`: 代码格式调整

### 示例
```bash
git commit -m "feat: 添加拼音搜索功能"
git commit -m "fix: 修复卡片翻转动画问题"
git commit -m "chore: 更新奥森公园鸟单"
git commit -m "docs: 更新 README 文档"
```

---

## ⚠️ 常见问题

### 1. 推送失败：rejected (non-fast-forward)

**原因**：远程有新的提交，本地落后了

**解决**：
```bash
# 先拉取远程改动
git pull --rebase

# 再推送
git push origin main
```

### 2. 推送失败：divergent branches

**原因**：本地和远程的历史分叉了

**解决**：
```bash
# 方法 1：rebase（推荐）
git pull --rebase
git push origin main

# 方法 2：强制推送（慎用！）
git push --force-with-lease origin main
```

### 3. 忘记先推送子模块

**现象**：推送主仓库后，GitHub 上显示子模块的 commit 找不到

**解决**：
```bash
# 补推子模块
cd feather-flash-quiz
git push origin main
```

### 4. 不小心提交了大文件

**解决**：
```bash
# 从 git 中删除但保留本地文件
git rm --cached 文件名

# 添加到 .gitignore
echo "文件名" >> .gitignore

# 提交删除
git add .gitignore
git commit -m "chore: 从 git 中移除大文件"
git push origin main
```

---

## 🎯 快速参考

### 完整推送流程（两个仓库都改了）

```bash
# 子模块
cd /Users/my/Desktop/Code/小鸟记忆卡/feather-flash-quiz
git add -A && git commit -m "前端改动" && git push origin main

# 主仓库
cd /Users/my/Desktop/Code/小鸟记忆卡
git add -A && git commit -m "主仓库改动" && git push origin main
```

### 检查是否有未推送的改动

```bash
# 主仓库
cd /Users/my/Desktop/Code/小鸟记忆卡
git status

# 子模块
cd feather-flash-quiz
git status
```

---

## 📌 注意事项

1. ✅ **子模块要先推送**，然后再推送主仓库
2. ✅ **推送前先检查**：`git status` 确认改动内容
3. ✅ **写清楚 commit 信息**：方便以后查找
4. ❌ **不要推送大文件**：图片、音频、node_modules 等已在 .gitignore 中
5. ❌ **不要用 `--force` 强制推送**（除非非常确定）

---

## 📚 相关文档

- [运行说明.md](./运行说明.md) - 项目运行和开发说明
- [README.md](./README.md) - 项目总体介绍

---

**最后更新**：2026-02-02
