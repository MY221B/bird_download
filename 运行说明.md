## 周更新流程

### 运行命令
```bash
bash tools/weekly_refresh_and_push.sh

## 0. 与 Lovable 协作的 Git 工作流程

### 项目结构说明
- 主仓库：`bird_download` (当前仓库)
- 子模块：`feather-flash-quiz` (网站前端项目)
  - `main` 分支：主开发分支，用于本地开发
  - `develop_lovable` 分支：Lovable 专用分支

### 📥 开始工作前：从 Lovable 同步改动

**方法一：使用便捷脚本（推荐）**
```bash
# 在主仓库根目录运行
bash tools/sync_from_lovable.sh
```

**方法二：手动操作**
```bash
cd feather-flash-quiz
git checkout develop_lovable
git pull origin develop_lovable
git checkout main
git pull origin main
git merge develop_lovable
git push origin main
cd ..
```

### 🛠️ 本地开发
```bash
cd feather-flash-quiz
git checkout main
# ... 进行你的改动 ...
git add .
git commit -m "your commit message"
git push origin main
```

### 📤 完成开发后：同步到 Lovable

**方法一：使用便捷脚本（推荐）**
```bash
# 在主仓库根目录运行
bash tools/sync_to_lovable.sh
```

**方法二：手动操作**
```bash
cd feather-flash-quiz
git checkout develop_lovable
git pull origin develop_lovable
git merge main
git push origin develop_lovable
git checkout main
cd ..
```

### 🔄 更新主仓库的子模块引用
```bash
# 在主仓库根目录
git add feather-flash-quiz
git commit -m "chore: update feather-flash-quiz submodule"
git push origin main
```

### 🧪 本地测试
```bash
cd feather-flash-quiz
npm run dev
```

---

## 1. 周更新流程（已集成 Lovable 自动同步）

### 运行命令
```bash
bash tools/weekly_refresh_and_push.sh
```

### 自动执行流程
   - ✅ **自动从 Lovable 拉取最新改动**（开始前）
   - 🔄 自动更新每个地点最近7天的鸟单
   - 📥 下载新的鸟种图片和数据
   - 📁 创建不同地点最新的文件夹并把鸟的 JSON 放进去
   - 🚀 推送到 GitHub
   - ✅ **自动同步到 Lovable 的 develop_lovable 分支**（结束后）

### 注意事项
   - 如果有新鸟，通常需要 20 分钟左右来下载并上传图片
   - 脚本会自动处理与 Lovable 的同步，**无需手动运行同步脚本**

### 新增地点
   - 如果要新增地点，可以先发送以下 prompt：
     ```
     请在birdreport_locations.json中添加地点"植物园"，使周更新时可以自动更新该地点
     ```
   - *注意自己再检查下别名是否正确，可手动删除或添加

## 2. 调试和图片筛选
    2.1 是否有下载或上传失败的新鸟，如果有失败的
        - 重新跑一遍 bash tools/weekly_refresh_and_push.sh
        - 如果还失败，就把失败的扔给ai，问为什么，慢慢debug
        - 或者直接自己去下载图片到本地，放在images里，然后重新跑一遍
    2.2 如果有新鸟上传成功
        - 需要去examples的html文件里，筛选一下是否有不合适的图片
        
        **方法一：使用自动化脚本（推荐）**
        1. 在html中选中要删除的图片信息，复制到 config/需要删除图片名单 文件中
        2. 运行自动化脚本：
           bash tools/delete_images_from_config.sh
        3. 脚本会自动完成以下操作：
           - 从 Cloudinary 删除图片
           - 从 JSON 文件清理引用
           - 删除本地图片文件
           - 重新生成 HTML 画廊
           - 自动提交并推送到 GitHub
        
        **方法二：手动使用工具脚本**
        - 把要删除的图片信息复制给ai，并输入以下prompt：
            请把以下图片从本地图片、@cloudinary_uploads 、
            @location_birds 、html引用以及其他任何出现的地方都删除
            可使用的工具脚本
            delete_cloudinary_by_list.py - 从 Cloudinary 删除
            cleanup_references_by_list.py - 从 JSON 文件清理引用
            delete_local_images_by_list.py - 删除本地图片文件
            update_gallery_from_cloudinary.py - 重新生成 HTML
        
        - 删除完之后重新跑一遍 bash tools/weekly_refresh_and_push.sh

## 3. 发布到生产环境

✅ **自动化脚本已自动同步到子模块的 develop_lovable 分支**

Lovable 监听子模块的 `develop_lovable` 分支，推送后会**自动部署**，无需手动操作。

## 4. 改动网页默认展示地点
    - 可以把这段prompt发给ai：（改默认地点）
        查看默认展示的地点的逻辑，把默认地点改成"颐和园"。
        关注 /Users/my/Desktop/Code/小鸟记忆卡/feather-flash-quiz/src/pages/Index.tsx 文件。
        改好后直接git push到github
    - 确认已改动并推送（子模块的 develop_lovable 分支）后，Lovable 会自动部署

## 5. 自动化删除图片脚本（已集成 Lovable 自动同步）
    - 脚本位置：tools/delete_images_from_config.sh
    - 配置文件：config/需要删除图片名单
    
    **使用步骤：**
    1. 在 examples/gallery_all_cloudinary.html 中找到需要删除的图片
    2. 复制图片的 public_id 信息（JSON格式）
    3. 粘贴到 config/需要删除图片名单 文件中
    4. 运行脚本：bash tools/delete_images_from_config.sh
    5. 确认删除后，脚本会自动完成所有删除和提交工作
    
    **脚本自动执行流程：**
    - ✅ **自动从 Lovable 拉取最新改动**（开始前）
    - ✅ 从 Cloudinary 云端删除图片
    - ✅ 从 cloudinary_uploads 目录清理 JSON 引用
    - ✅ 从 location_birds 目录清理 JSON 引用
    - ✅ 删除 images 目录下的本地图片文件
    - ✅ 重新生成 HTML 画廊
    - ✅ 自动 Git 提交并推送到 GitHub
    - ✅ **自动同步到 Lovable 的 develop_lovable 分支**（结束后）
    
    **注意事项：**
    - 脚本会在执行前显示待删除图片数量并要求确认
    - 可以通过参数指定不同的配置文件：`bash tools/delete_images_from_config.sh 其他配置文件路径`
    - 删除操作不可逆，请确认后再执行
    - 脚本会自动处理与 Lovable 的同步，**无需手动运行同步脚本**
    - 推送到子模块 develop_lovable 后，Lovable 自动部署，无需手动操作